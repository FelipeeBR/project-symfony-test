{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.size) }}
    {{ form_row(form.responsible) }}
    {{ form_row(form.veterinarian) }}
    {{ form_row(form.save, {'label': 'Adicionar'}) }}
{{ form_end(form) }}
