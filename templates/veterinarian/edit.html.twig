{% extends 'base.html.twig' %}

{% block title %}Editar Veterinario {{ veterinarian.name }}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="h3 mb-3">Editar veterinário: {{ veterinarian.name }}</h1>
        <div class="d-flex justify-content-end mb-3 gap-2">
            <a href="{{ path('veterinarian_index') }}" class="btn btn-secondary btn-sm">
                <i class="fas fa-arrow-left"></i> Voltar
            </a>
            {{ include('veterinarian/_delete_form.html.twig') }}
        </div>
        <div class="card">
            <div class="card-header">
                <h3 class="h3 mb-0">Preencha os campos</h3>
            </div>
            
            <div class="card-body">
                {{ include('veterinarian/_form.html.twig', {'button_label': 'Atualizar'}) }}
            </div>
        </div>
    </div>
{% endblock %}